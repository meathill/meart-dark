<!DOCTYPE html>
<html lang="zh-cmn-Hans">
<head>
  <meta charset="UTF-8">
  <title>{{siteTitle}}</title>
  <meta name="description" content="{{siteDesc}}">
  <meta name="keywords" content="{{siteTags}}">
  <meta name="viewport" content="initial-scale=1.0;user-scale=no">
  <meta name="robots" content="index,follow">
  {{#if siteIcon}}
    <link rel="shortcut icon" type="image/png" href="./img/fav.png">
  {{/if}}
  <link rel="stylesheet" href="//cdn.staticfile.org/twitter-bootstrap/4.0.0-alpha.5/css/bootstrap.min.css">
  <link rel="stylesheet" href="css/screen.css">
</head>
<body>
{{> header}}
<div id="photo-list" class="container">
  <h1>{{title}}</h1>
  <p class="lead">{{description}}</p>
  <ul>
    {{#each album}}
    <li>
      <a href="{{src}}" data-description="{{description}}">
        <img src="{{src}}" alt="{{title}}" width="{{width}}" height="{{height}}">
      </a>
    </li>
    {{/each}}
  </ul>
  <footer>
    <small>
      Create at: <time datetime="{{toDate createTime}}">{{toDate createTime}}</time> |
      Modified at: <time datetime="{{toDate lastModifiedTime}}">{{toDate lastModifiedTime}}</time>
    </small>
  </footer>
</div>
{{> footer}}
</body>
</html>